import React from 'react';
import ReactDOM from 'react-dom';
// import './index.css'; Not using custom css
import App from './App';

// Renders our app and puts it in the root in the html file.
ReactDOM.render(<App />, document.getElementById('root'));